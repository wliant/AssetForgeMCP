version: "3.8"
services:
  asset-forge-mcp:
    build: .
    ports:
      - "8091:8091"
    env_file:
      - .env
    environment:
      - MCP_PORT=8091
    volumes:
      - asset-data:/app/assets/generated
    restart: unless-stopped

volumes:
  asset-data:
