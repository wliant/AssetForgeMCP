version: "3.8"
services:
  asset-forge-mcp:
    build: .
    ports:
      - "9500:9500"
    env_file:
      - .env
    environment:
      - MCP_PORT=9500
    volumes:
      - asset-data:/app/assets/generated
    restart: unless-stopped

volumes:
  asset-data:
